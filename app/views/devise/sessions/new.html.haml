.devise_actions_wrapper.margin_auto
  .devise_actions_header
    %h2 UZEMI にサインイン
    %p uzemi.slack.com
  %p.text-left
    <strong>メールアドレス</strong>と<strong>パスワード</strong>を入力してください
  = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
    .field
      = f.email_field :email, autofocus: true, autocomplete: "email"
    .field
      = f.password_field :password, autocomplete: "current-password"
    .actions
      = f.submit "サイトイン"
    - if devise_mapping.rememberable?
      .field.text-left
        = f.check_box :remember_me
        = f.label :remember_me, "ログイン情報を記録する"

  - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
    = link_to "パスワードを忘れてしまった？", new_password_path(resource_name)
    %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "アカウントはこちらから作成できます", new_registration_path(resource_name)
  %br/

