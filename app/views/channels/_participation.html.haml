.message.flex
	- if (channel_user.user.avator.attached?)
		= image_tag(channel_user.user.avator, class: 'thumb_36')
	- else
		= image_tag('default_prof1.png', class: 'thumb_36')
	.message__contents
		.message__user
			%h4 #{channel_user.user.name}
			%p.message__time #{channel_user.created_at.strftime('%H:%M')}
		%p.message__notice ##{channel_user.channel.name} に参加しました。
	-# %p.message__notice #generalに参加しました。